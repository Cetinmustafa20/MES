<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Giriş Yap</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
      color: white;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', sans-serif;
    }
    .card {
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.3);
      background-color: #1c1c1e;
      width: 100%;
      max-width: 400px;
    }
    .form-control {
      background-color: #2a2a2e;
      color: white;
      border: 1px solid #444;
    }
    .form-control::placeholder {
      color: #aaa;
    }
    .btn-primary {
      background-color: #0dcaf0;
      border: none;
    }
    .btn-primary:hover {
      background-color: #31d2f2;
    }
    a {
      color: #0dcaf0;
    }
  </style>
</head>
<body>
  <div class="card text-white">
    <h2 class="text-center mb-4">🔐 Giriş Yap</h2>

    <% if (typeof hata !== "undefined") { %>
      <div class="alert alert-danger"><%= hata %></div>
    <% } %>

    <form action="/login" method="POST">
      <div class="mb-3">
        <label for="username" class="form-label">Kullanıcı Adı</label>
        <input type="text" name="username" class="form-control" id="username" required placeholder="admin123">
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Şifre</label>
        <input type="password" name="password" class="form-control" id="password" required placeholder="••••••••">
      </div>
      <div class="d-grid mb-2">
        <button type="submit" class="btn btn-primary">Giriş Yap</button>
      </div>
      <p class="text-center">Hesabınız yok mu? <a href="/register">Kayıt Ol</a></p>
    </form>
  </div>
</body>
</html>
