<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>MES Ana Sayfa</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .navbar-brand {
      font-weight: bold;
      font-size: 1.4rem;
    }
    .card-hover:hover {
      transform: scale(1.03);
      transition: transform 0.2s ease-in-out;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">MES Sistemi</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a href="/" class="nav-link">🏠 Ana Sayfa</a></li>
          <li class="nav-item"><a href="/admin" class="nav-link">🛠️ Admin Paneli</a></li>
          <li class="nav-item"><a href="/ekle" class="nav-link">➕ Yeni Kayıt</a></li>
          <li class="nav-item"><a href="/logout" class="nav-link">🚪 Çıkış</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- İçerik -->
  <div class="container mt-4">
    <h3 class="mb-4">📂 Modüller</h3>
    <div class="row g-4">

      <!-- Mesajlaşma -->
      <div class="col-md-3">
        <a href="/mesajlasma/<%= typeof roomId !== 'undefined' ? roomId : 'genel' %>" class="text-decoration-none text-dark">
          <div class="card card-hover shadow-sm h-100">
            <div class="card-body text-center">
              <h1>💬</h1>
              <h5 class="card-title mt-2">Mesajlaşma</h5>
            </div>
          </div>
        </a>
      </div>

      <!-- Diğer modüller -->
      <div class="col-md-3">
        <div class="card shadow-sm h-100 text-center">
          <div class="card-body text-muted">
            <h1>📦</h1>
            <h5 class="card-title mt-2">Stok</h5>
          </div>
        </div>
      </div>

      <div class="col-md-3">
        <div class="card shadow-sm h-100 text-center">
          <div class="card-body text-muted">
            <h1>📊</h1>
            <h5 class="card-title mt-2">Raporlar</h5>
          </div>
        </div>
      </div>

      <div class="col-md-3">
        <div class="card shadow-sm h-100 text-center">
          <div class="card-body text-muted">
            <h1>👷</h1>
            <h5 class="card-title mt-2">Personel</h5>
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
